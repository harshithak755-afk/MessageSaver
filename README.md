# MessageSaver
Developed a Django web application that securely stores user messages using authentication and session management. Implemented login/logout system, protected routes, and cookie handling to ensure safe access to user data.
